apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  labels:
    app: nginx-sts
spec:
  type: NodePort
  selector:
    app: nginx-sts
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
    nodePort: 30080
