apiVersion: v1
kind: Service
metadata:
  name: nginx-sticky
  labels:
    app: nginx-sts
spec:
  type: ClusterIP
  selector:
    app: nginx-sts
  sessionAffinity: ClientIP
  sessionAffinityConfig:
    clientIP:
      timeoutSeconds: 3600
  ports:
  - protocol: TCP
    port: 80
    targetPort: 80
